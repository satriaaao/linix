const fs=require('fs'),z=require('zlib'),p=require('path');let b='';for(let i=1;i<=6;i++)b+=fs.readFileSync('vbundle.'+i,'utf8');const data=JSON.parse(z.gunzipSync(Buffer.from(b,'base64')).toString('utf8'));for(const [f,c] of Object.entries(data)){fs.mkdirSync(p.dirname(f),{recursive:true});fs.writeFileSync(f,c)}console.log('BSM source restored:',Object.keys(data).length,'files');
